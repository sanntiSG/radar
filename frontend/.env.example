# URL del backend de Radar
NEXT_PUBLIC_API_URL=http://localhost:4000

# Client ID de Google OAuth (el mismo que GOOGLE_CLIENT_ID del backend).
# Si queda vacío, el login con Google se muestra deshabilitado (el modo demo siempre funciona).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
