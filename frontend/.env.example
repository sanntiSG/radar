# ============================================================
# RADAR — Frontend Environment Variables
# Copiar este archivo como .env.local
# ============================================================

# URL del backend (cambiar en producción por la URL de Render)
NEXT_PUBLIC_API_URL=http://localhost:4000
